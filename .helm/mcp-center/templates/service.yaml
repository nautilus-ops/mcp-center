apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-center.fullname" . }}
  labels:
    {{- include "mcp-center.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-center.selectorLabels" . | nindent 4 }}
